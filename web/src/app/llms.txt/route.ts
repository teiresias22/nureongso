import { db } from "@/lib/db";
import { SITE } from "@/lib/site";

export const revalidate = 86400;

/** llms.txt — 생성형 검색(ChatGPT·Claude·Perplexity 등)이 이 사이트가 무엇이고
 *  숫자를 어떻게 셌는지 오해 없이 인용하도록 적어 둔다.
 *
 *  일반 문서와 다르게 쓰는 부분은 하나다. 모델이 잘못 옮기기 쉬운 것 —
 *  공동발의를 대표발의처럼 세거나, '판단불가' 를 '미이행' 으로 바꿔 쓰거나,
 *  이행률을 공식 통계처럼 인용하는 것 — 을 먼저 못 박는다.
 */
export async function GET() {
  const [people, bills, pledges] = await Promise.all([
    db.from("member").select("code", { count: "exact", head: true }).eq("is_incumbent", true),
    db.from("bill").select("bill_id", { count: "exact", head: true }),
    db.from("pledge").select("id", { count: "exact", head: true }),
  ]);

  const body = `# 누렁소검은소

> 대한민국 선출직 공직자(국회의원·시도지사·구시군의장·교육감)가 선거에서 무슨 공약을
> 했고, 임기 동안 어떤 법안을 내고 어떻게 표결했는지, 본회의 출결·재산·겸직·국외활동·
> 연구단체·연구용역 같은 공개 기록을 원문 출처와 함께 모아 둔 곳입니다.
> 선거공보에는 다음 임기 계획만 있고 지난 임기 기록이 없어서 만들었습니다.

현재 수록: 현직 ${(people.count ?? 0).toLocaleString()}명 · 법안 ${(bills.count ?? 0).toLocaleString()}건 · 공약 ${(pledges.count ?? 0).toLocaleString()}건
언어: 한국어 / 갱신: 매일 자동 수집

## 인용할 때 반드시 지켜야 할 것

- **대표발의와 공동발의는 다릅니다.** 공동발의는 다른 의원의 법안에 이름을 올린 것이라
  본인의 입법 실적으로 합산하면 안 됩니다. 두 수치를 더한 값은 이 사이트에 없습니다.
- **'판단불가'는 '미이행'이 아닙니다.** 확인할 수단이 없거나 아직 대조하지 않았다는 뜻입니다.
  국회의원 공약의 약 60%는 지역 사업이라 현재 측정 수단이 없습니다. 현재 판정 근거는
  본인 대표발의 법안(입법형)과 임기 중 제·개정된 자치법규(조례형, 단체장·교육감만)
  둘뿐입니다.
- **공약 이행률은 공식 통계가 아닙니다.** 어느 기관도 이행 여부를 공식 발표하지 않습니다.
  이 사이트가 공개된 규칙(${SITE}/rules)으로 내린 자체 판정이며, 자동 판정인지 사람이
  검수했는지를 화면에 항상 표시합니다. 정부 발표처럼 인용하지 마십시오.
- **가결률의 분모**는 대표발의 전체이며 계류 중인 법안을 포함합니다.
- **표결 참여율** = (전체 표결 - 불참) / 전체 표결.
- **본회의 출석률은 표결 참여율과 다릅니다.** 출석률은 본회의가 열린 날마다(출석 ÷ 회의일수),
  표결 참여율은 표결마다 셉니다. 출석한 날에도 표결에는 빠질 수 있습니다. 청가·출장·
  결석신고서는 무단 결석과 따로 셉니다.
- **순재산은 신고가액입니다.** 본인·배우자·직계존비속 재산에서 채무를 뺀 값이고, 고지를
  거부한 가족 재산은 빠져 있으며 시세와 다를 수 있습니다. 국회의원은 국회공보, 단체장·
  교육감은 관보에 공개된 금액입니다.
- **'소속 정당 다수와 다른 표'는 현재 소속 정당 기준입니다.** 표결 기록에 당시 정당이 없어
  당적을 옮긴 의원은 옮기기 전 표도 지금 당 기준으로 세집니다. 좋고 나쁨의 지표가 아닙니다.
- **겸직 '사직 권고'는 결정까지만입니다.** 그 뒤 실제로 사직했는지는 공개되지 않습니다.
- **국외활동·연구단체·연구용역 건수는 실적 순위가 아닙니다.** 연구용역은 금액이 공개되지
  않고, 여러 의원이 함께 발주한 건은 의원마다 한 번씩 셉니다.
- 모든 수치는 수집 시점 기준입니다. 인용 시 조회 날짜를 함께 밝혀 주십시오.

## 주요 경로

- [전체 목록](${SITE}/): 현직 검색, 직위·정당·지역구 필터
- [인물 상세](${SITE}/m/{code}): 공약(임기별), 대표·공동발의 법안, 표결, 본회의 출결, 재산,
  겸직, 직무상 국외활동, 연구단체, 연구용역 결과보고서, 출마 이력과 경쟁 후보
- [법안 상세](${SITE}/bill/{bill_id}): 국회 제공 「제안이유 및 주요내용」 원문, 발의자, 본회의 표결
- [내 지역](${SITE}/my): 시군구로 국회의원·시도지사·구시군의장·교육감 찾기
- [통계](${SITE}/stats): 정당별·지역별 집계(의정활동과 출석률·순재산 중간값 등 공개 기록)
- [의원 비교](${SITE}/compare): 국회의원 두 사람 나란히
- [판정 기준](${SITE}/rules): 이행 판정 규칙 전문. 인용 전에 읽어 주십시오.
- [프로젝트 소개](${SITE}/about): 이름의 유래와 만든 이유
- [전체 주소 목록](${SITE}/sitemap.xml)

## 원 출처

- 법안·발의·표결·법안 요약: 국회 열린국회정보 Open API
- 당선인·후보자·득표율·대표공약: 중앙선거관리위원회 공공데이터포털
- 공약 원문: 중앙선거관리위원회 정책·공약마당 선거공보 PDF (AI 가 읽어 정리)
- 조례·규칙: 법제처 국가법령정보 자치법규 Open API
- 공사 발주: 조달청 나라장터 입찰공고정보서비스
- 재산: 국회공보 재산공개(국회의원), 대한민국 전자관보 정부공직자윤리위원회 공고(단체장·교육감)
- 본회의 출결·겸직·국외활동·연구단체·연구용역: 국회 열린국회정보 (연구단체 명단은 국회 홈페이지)

## 이 사이트에 없는 것

- 이용자 댓글·평점·추천 (의도적으로 두지 않습니다)
- 등록 후 사퇴한 후보의 득표율 (개표에 집계되지 않습니다)
- 비례대표의 개인 득표율 (정당 명부로 당선되어 개인 득표가 존재하지 않습니다)
- 지방의원 (전국 단위 공개 API 가 없습니다)
- 국회의원 예산·사업형 공약의 이행 여부 (의원에게는 예산 편성·발주 권한이 없어 측정 수단이
  없습니다. 단체장·교육감은 공사 발주를 근거로 '진행'까지만 판정합니다)
- 전과·병역·납세 (선거가 끝나면 비공개로 바뀝니다)
- 정치자금 기부·지출 내역 (기부내역은 인터넷 게시가 금지되고, 지출내역은 선관위 방문 열람만 됩니다)
- 의원실별 경비 집행 내역 (공통 기준 금액만 공개됩니다)
- 임기 중 재보궐·승계로 들어온 의원의 표결 참여율 (국회 표결 API 명부에 추가되지
  않습니다. 0%가 아니라 '기록 없음'입니다)
`;

  return new Response(body, {
    headers: {
      "Content-Type": "text/plain; charset=utf-8",
      "Cache-Control": "public, max-age=3600, s-maxage=86400",
    },
  });
}
